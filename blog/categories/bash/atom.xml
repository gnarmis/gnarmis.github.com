<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: bash | Kilotau]]></title>
  <link href="http://gnarmis.github.com/blog/categories/bash/atom.xml" rel="self"/>
  <link href="http://gnarmis.github.com/"/>
  <updated>2013-01-08T18:56:09-06:00</updated>
  <id>http://gnarmis.github.com/</id>
  <author>
    <name><![CDATA[Gursimran Singh]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Sass and CoffeeScript - Compile and Watch Files with One Command]]></title>
    <link href="http://gnarmis.github.com/blog/2011/08/09/sass-and-coffeescript-compile-and-watch-files/"/>
    <updated>2011-08-09T16:30:00-05:00</updated>
    <id>http://gnarmis.github.com/blog/2011/08/09/sass-and-coffeescript-compile-and-watch-files</id>
    <content type="html"><![CDATA[<p>Do you use Sass and CoffeeScript? Tired of remembering and typing out the compile
and watch commands on both of those command-line utilities? I used to do:</p>

<p>[code] compass watch <br/>
 coffee -o scripts/ -cw coffee/ [/code]</p>

<p>...on all of my projects, in separate tabs on the terminal. There had to be
an easier way.</p>

<p>Do them both, with the standard configuration, using the script below.</p>

<p>```bash</p>

<h1>!/bin/bash</h1>

<h1>run <code>compass watch</code> at pwd</h1>

<h1>run <code>coffee -o scripts/ -cw coffee/</code> at pwd</h1>

<p>type -P compass &amp;>/dev/null  || { echo "Compass command not found."; exit 1; }
type -P coffee &amp;>/dev/null  || { echo "Coffee command not found."; exit 1; }</p>

<p>if [ ! -d sass/ ] || [ ! -d scripts/ ]
then
  echo "Project not setup correctly! Put sass files in sass/ and coffee in coffee/"
else
  if [ ! -d stylesheets/ ]
  then</p>

<pre><code>mkdir stylesheets
</code></pre>

<p>  fi
  if [ ! -d scripts/ ]
  then</p>

<pre><code>mkdir scripts
</code></pre>

<p>  fi
  echo "Watching changes in sass/ and coffee/ and compiling to stylesheets/ and scripts/ ..."
  <code>compass watch --quiet</code> &amp;
  <code>coffee -o scripts/ -cw coffee/</code> &amp;
  wait
fi
```</p>

<p>Add the script into your project, do</p>

<p>[code]chmod +x sassycoffee.sh[/code]</p>

<p>to make it executable, and forget about it! I'm planning on some edits in the
future, but it works well enough and gets the job done. Now, all you need to
do is:</p>

<p>[code]./sassycoffee.sh[/code]</p>

<p>This was a quick little hack in bash but pretty helpful for my workflow.</p>

<p>Tips? Improvements? I'd be glad to hear 'em.</p>

<p>Update: https://github.com/gnarmis/SassyCoffee</p>
]]></content>
  </entry>
  
</feed>
